{% macro aside(image, alt, description, classList = []) %}
<figure class="aside">
  <img class="aside__visual" src="{{ image }}" alt="{{ alt }}">
  {% if description != "" %}
    <figcaption class="aside__caption">{{ description }}</figcaption>
  {% endif %}
</figure>
{% endmacro %}
